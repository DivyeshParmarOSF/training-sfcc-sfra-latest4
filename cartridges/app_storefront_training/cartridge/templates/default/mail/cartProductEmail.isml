<body>

    <h1>${Resource.msg('email.subject.new.prodcut.add', 'cart_product', null)}&nbsp;${pdict.firstName}&nbsp;${pdict.lastName},</h1>
    
    <iscomment> <p>${Resource.msg('email.msg.new.registration.body1','cart_product',null)}</p>    </iscomment>
    
    <p><a href="${pdict.url}">${pdict.url}</a></p> 
    
    <iscomment> <p>${Resource.msg('email.msg.new.registration.body2','cart_product',null)}</p> </iscomment>

</body>